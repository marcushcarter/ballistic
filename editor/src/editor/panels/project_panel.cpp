#include "project_panel.h"
#include "../editor_context.h"

void ProjectPanel::Draw(EditorContext& ctx)
{
    // TODO: move from Editor::DrawProjectPanel
    (void)ctx;
}

void ProjectPanel::DrawAddImagePopup(EditorContext& ctx)
{
    // TODO: move from the inline popup inside DrawProjectPanel
    (void)ctx;
}
