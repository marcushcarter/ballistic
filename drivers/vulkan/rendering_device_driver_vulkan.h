
// #include <thirdparty/re-spirv/re-spirv.h>
// #include <thirdparty/vulkan/vk_mem_alloc.h>
// includes ballistic vulkan

class RenderingDriverDeviceVulkan {
    struct Queue {
        int virtualCount = 0;
    };

    struct SubgroupCapabilities {

    };

    struct ShaderCapabilities {

    };

    struct StorageBufferCapabilities {

    };

    struct RaytracingCapabilities {

    };

    // vkDevice
    // rendering context vulkan
    // rendering context driver 
    // frame count 

    struct  BufferInfo {
        // buffer etc etc
    };
    

    struct TextureInfo {
        // vkImage, image view, rd fromta, creatinfo, iv ci, vma allocation, allocation info, bool issampled, bool createdfromextension, bool transient
    };

    // all vulkan object creators are literally here

    // set_object_name
    // get resource handle
}