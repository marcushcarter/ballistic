#pragma once
#include <cstdint>
#include <string>
#include <filesystem>


namespace ballistic {

struct ProjectSettings {

    // std::string name;
    // std::string description;
    // std::string version "1.0.0";
    // std::filesystem::path icon;
    // std::filesystem::path windows_icon;

    // uint32_t with = 1280, height = 720;
    // int init_x = -1, init_y = -1;
    // int vsync_mode = 1;
};
    
}